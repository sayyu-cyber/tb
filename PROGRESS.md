# Thaasbai — Build Status
*Generated 2026-07-25 from the actual codebase at github.com/sayyu-cyber/tb (branch `main`, HEAD `d6a7256`) plus uncommitted local changes.*

## What broke, and why

Login on https://thasbai.netlify.app stopped working after "workflow 3." The site's code was never actually broken — commit `d6a7256` is the same working app from workflow 2. What changed was the **live Firestore security rules**, deployed directly from your machine via `firebase deploy` (this bypasses GitHub/Netlify entirely, which is why the deploy history looked clean).

Workflow 3 added a new `firestore.rules` file scoped only to the new economy feature:

```
match /playerEconomy/{uid} {
  allow read, write: if request.auth != null && request.auth.uid == uid;
}
```

and ran `firebase deploy` (visible in workflow 3.docx around the `firebase.json`/`firestore.rules` setup steps). That overwrote whatever rules existed before. But your login flow (`contexts/AuthContext.tsx`) reads and writes a **different** collection — `players/{uid}` — on every sign-in to fetch/init player stats. Since the new rules never mention `players`, every login now gets a Firestore "permission denied" the instant it tries to load stats, which is what you're seeing as "can't log in."

**Fix applied:** `firestore.rules` in your project folder now allow both collections:

```
match /players/{uid}      { allow read, write: if request.auth != null && request.auth.uid == uid; }
match /playerEconomy/{uid} { allow read, write: if request.auth != null && request.auth.uid == uid; }
```

**This still needs to be deployed** — I can't do this step for you since it requires your own Firebase CLI login. From the `thaasbai` project folder on your machine:

```
firebase deploy --only firestore:rules
```

That's the only step needed to restore login. No GitHub push or Netlify rebuild is required for this specific fix.

## Other things found while investigating

- `contexts/EconomyContext.tsx`, `package.json`/`package-lock.json` (adds `firebase-admin`, `firebase-functions`), `firebase.json`, `.firebaserc`, `firestore.indexes.json`, and `functions/` all exist locally but were **never committed** — they're sitting as uncommitted/untracked changes in your project folder. This is real, correct progress (the Economy context is properly wired to Firestore, game-economy shaped — not the unrelated "budget/transactions" version workflow 3's chat first hallucinated before you course-corrected). Worth committing once you've verified it builds cleanly, so future work isn't at risk of being lost again.
- `functions/` (Cloud Functions for awarding coins server-side) had TypeScript build errors in the workflow 3 transcript (`admin.firestore` typing issues from a `firebase-admin`/`firebase-functions` version mismatch) that don't look fully resolved — check `functions/src/index.ts` before deploying functions.
- Leaderboard (`hooks/useLeaderboard.ts`) is still mock data, not live Firestore — flagged below too.

## Status vs. the GDD roadmap

### Alpha
- Authentication (Google, email, guest) — **Done**, fixed and confirmed working live (2026-07-26).
- Profiles — **Done** (profile page, stats). Customizable (2026-07-27): players can edit their username and pick an avatar color and profile banner from a preset gallery (`constants/profileCustomization.ts`, `components/profile/EditProfileModal.tsx`) via a new edit button on the Profile page. Deliberately preset-based rather than free-form image upload - this app has no Firebase Storage configured, and adding it would mean new Storage rules (another manual paste-in-console step for you) plus upload validation; presets deliver real customization today with zero new infrastructure. Choices are stored on the existing `players/{uid}` doc (no new Firestore rules needed) and show up on the public profile page too. Guest accounts can preview changes but they aren't saved (guests were never actually signed into Firebase to begin with).
- Mindi — **Done** (2026-07-26). Real rules implemented in `lib/mindiEngine.ts` + `components/game/MindiGameClient.tsx`, based on the documented Maldivian "Dihaeh" ruleset (pagat.com/national/maldives.html, research by Alex de Voogt): 4 players in 2 fixed partnerships, 13 cards each, last card dealt sets trump, follow-suit trick-taking, first team to capture 3 of the 4 Tens wins (4 Tens = "baga", all 13 tricks = "hukunbunye"). AI mode = you + 3 bots; Pass & Play = you + a local partner (seats 0 & 2) vs 2 bots, with a "pass the device" screen between turns.
- Gin Rummy — **Done** (2026-07-26). Real rules in `lib/ginRummyEngine.ts` + `components/game/GinRummyGameClient.tsx`: standard 2-player Gin Rummy — 10-card hands, draw/discard, meld detection (sets + runs) via a deadwood-minimizing solver, knock/gin/undercut scoring, lay-off on knock. AI mode = you vs a bot; Pass & Play = 2 local players with a pass-the-device screen. Verified with a 500-hand bot-vs-bot simulation (deck integrity, legal moves, no negative scores, no infinite loops) before shipping.
- Casual mode — **Done** for both games now (real Mindi + real Gin Rummy, AI and Pass & Play). The old placeholder mini-game and its components have been removed.
- Leave/forfeit warning — **Done** (2026-07-27). New `components/game/LeaveMatchButton.tsx` replaces the plain "back to /play" button on all four gameplay screens (Mindi/Gin Rummy × AI-or-Pass&Play/Ranked-or-Room - Room matches reuse the same online clients as Ranked, so this covers both automatically). Casual (AI/Pass & Play) shows a "progress will be lost" warning; Ranked/Room shows a forfeit warning and, if confirmed, ends the match immediately with the opponent(s) awarded the win and the leaver's own loss recorded right away (since they won't be around to claim it themselves). Added `"forfeit"` as a `HandOutcome.special` value (Mindi) and an optional `forfeitedBy` field (Gin Rummy) so the existing outcome/rewards screens handle it with no other logic changes needed.

### Beta
- Ranked mode — **Done** (2026-07-26), with one accepted trade-off. Real matchmaking (`lib/matchmaking.ts`) pairs real waiting players via a Firestore queue + transaction (2 for Gin Rummy, 4 for Mindi), then plays out live on a shared match document synced via `MindiOnlineClient`/`GinRummyOnlineClient`. **Trade-off:** there's no game server — match state lives in a Firestore doc both players can read, so a technically savvy opponent could inspect raw network data to see your hand even though the UI never shows it. Hardening this needs Cloud Functions to hold hands server-side (bigger project, deliberately deferred). There's also a small accepted race: if two matches try to form from overlapping queued players at the same instant, one player can end up briefly double-booked. Both are documented at the top of `lib/matchmaking.ts`. **Not yet tested with real multiple accounts** - I verified the code compiles and lints clean, but this needs an actual two/four-account live test, which I can't do from here.
- Trophies — **Done**.
- Daily/weekly match limits — **Fixed** (2026-07-27). Two real bugs found while refining Version 1: (1) `hooks/useMatchLimits.ts` tracked usage in localStorage but never actually compared it to the current day/week, so once a player used their 3 free daily matches, "Daily Left" stayed at 0 forever - it now resets correctly using the same day/week keys as Weekend League. (2) the daily/weekly counts were only ever *displayed*, never enforced - a player could keep queueing past 0 remaining. `RankedQueueClient` now blocks queueing once either cap is hit, with a clear "come back tomorrow/next week" screen. Also fixed: VIP's higher daily cap (`VIP_DAILY_MATCHES`) was never actually applied because `isVip` was a separate, never-set localStorage flag - it now reads real VIP status from `EconomyContext`. Trade-off: this is still a client-side/localStorage limit (same trust model as the rest of this Firestore-only app), not server-enforced - a technically savvy player could clear it, same caveat as documented for matchmaking.
- Leaderboards — **Done** (2026-07-26). Real Firestore query, ordered by trophies, confirmed working live.

### Version 1.0
- Dark/Light mode — **Done** (2026-07-27), full retrofit. The Settings toggle existed but did nothing (`darkTheme` was saved but never read anywhere). Replaced the ~480 hardcoded hex color classes across ~57 files with CSS variables (`styles/globals.css`: `--c1`..`--c5` for background/surface/border/muted-text/secondary-text, plus `--text-primary`), each with a dark value (default) and a light override under an `html.light` class. `contexts/SettingsContext.tsx` now actually toggles that class when the setting changes; `app/layout.tsx` has a small inline script that applies the saved theme before first paint so returning light-theme users don't see a flash of dark theme. Gold brand accents (`#D4AF37` and its gradient variants) intentionally stay identical in both themes - only the dark neutral "chrome" (backgrounds, borders, muted text) flips. Rank tier colors (Bronze/Silver/Gold/Platinum), rarity colors, suit colors, and avatar/banner cosmetic presets are untouched by design - they're meaningful data colors, not theme chrome. Verified with `npx tsc --noEmit` and `npx next lint` (no errors) plus a manual audit confirming no unintended replacements (e.g. dark ink text on gold buttons, which must stay dark regardless of theme, was deliberately excluded). Not visually tested in a live browser from here - worth a quick look on your end after deploying, especially the light-mode contrast on the game tables.
- Weekend League — **Done** (2026-07-27, standings page + nav tab added 2026-07-27). Friday-Saturday lock window (`hooks/useRankLock.ts`, fixed a bug where the "is Weekend League" flag was never true) now routes Silver+ players into a separate "weekend" matchmaking pool (`lib/matchmaking.ts`) instead of closing Ranked outright; sub-Silver players see a clear "keep climbing" message instead of a dead end. Wins/losses during Weekend League matches count double toward each player's `weeklyTrophies` (`lib/trophyUpdates.ts`), which resets lazily per-player at the start of their first match each week — no scheduled job needed. New `/tournament` page (reachable from a "Weekend" bottom-nav tab) shows live status, qualification, a countdown, direct queue buttons when the window is open, and this week's standings (`lib/weekendLeague.ts`'s `getWeeklyStandings`, tap a row to view that player's profile). **Known gap**, documented in the file: no scheduled job snapshots the bracket or crowns a permanent "Weekend Champion" at the exact end of the window; standings are a live proxy instead. No new Firestore rules needed. Not yet tested live with real qualified accounts.
- Friends — **Done** (2026-07-26, profile page added 2026-07-27). `lib/friends.ts` + `app/friends`: search players by username, send/accept/decline/cancel requests, friends list, and a "room invite" notification that lets a friend jump straight into a private room together. Tapping a friend, a Leaderboard row, or a Hall of Fame row now opens a real public profile page (`/player?uid=`, `lib/publicProfile.ts`) showing trophies, peak trophies, rank, win rate, and favorite game — closes the gap noted when Friends first shipped. Still not done: match history (a list of past matches). Home screen's "Friends"/"Private Rooms"/"Cosmetic Shop"/"Hall of Fame" placeholder cards now link to the real features; VIP Pass real payment is still genuinely not built, so left as "Coming Soon" (Weekend Tournament placeholder was replaced since Weekend League is real now).
- Rooms — **Done** (2026-07-26). Real private rooms in `lib/rooms.ts` + `components/game/RoomLobbyClient.tsx`: create a room (optional password), share the 6-character code, friends join by code, owner can kick, and starting the match hands off to the exact same live match system Ranked mode uses. Reachable from the game select card's new "Private Room" button. Same no-server-authority trust model as matchmaking (documented in the file). Not yet tested with real multiple accounts. Note: `app/room-cards` / `components/roomcards/RoomCardManager.tsx` is a *different, unrelated* feature despite the similar name - it's a consumable economy entitlement ("Room Card" item that unlocks unlimited private rooms for a time window), not the room system itself. The two aren't currently wired together (room creation isn't gated by owning a Room Card yet) - worth deciding whether that gating should exist.
- VIP — **Partial**. VIP UI components exist and are wired into the Economy context; real payment/subscription flow not present.
- Shop — **Done** UI + coin-spending logic wired to Economy context; real-money purchases not implemented (coins only).

### Version 1 feature request (in progress)
- Ranked split into 1v1/duo pools — **Done** (2026-07-27). Per-game split, confirmed with you before building: **Gin Rummy** keeps its existing 1v1 Ranked queue unchanged and adds a second, independent "Ranked 2v2" queue reached via a new pre-match "party" step (`components/game/RankedDuoClient.tsx` + `app/(main)/play/[game]/ranked-duo`) - two players team up in a 2-person room (reusing `lib/rooms.ts`'s room system with a new `mode: "rankedDuo"`), then both queue together (`lib/matchmaking.ts`'s new `joinDuoQueue`/`tryFormDuoMatch`). Because classic Gin Rummy has no real 4-player ruleset, "2v2" here is implemented as **two simultaneous, independent 1v1 sub-matches** between the crossed duo members (each match keeps its own win/loss, not a combined team score) - reuses `GinRummyOnlineClient` completely unchanged, which was the lowest-risk option. **Mindi** is inherently always a 4-seat, 2-partnership game, so per your direction its Ranked queue no longer randomly assigns a partner - a player must bring a partner through the same duo-party flow; the two duos become one 4-seat match with each duo kept together as one fixed partnership (`tryFormDuoMatch`'s seat order `[myDuo0, theirDuo0, myDuo1, theirDuo1]` matches `mindiEngine.ts`'s `teamOf()` grouping seats 0/2 and 1/3). `RankedQueueClient` now redirects Mindi's old solo-queue screen to the duo flow with an explanation; Weekend League Mindi matchmaking is intentionally left solo/unchanged, since you didn't ask for that mode to change and it's a separate, higher-stakes queue. No new Firestore rules needed - both `matchmakingQueue/{uid}` (new `partyId` field) and `rooms/{code}` (new `mode` field) are additions to existing, already-covered collections. **Not yet tested live with real multiple accounts.**

- Room redemption via Room Cards + kick/ban — **Done** (2026-07-27). Room creation now requires an active Room Card - `RoomLobbyClient`'s "Create a Room" step checks `getActiveRoomCards()` and, if none are active, shows a direct link to `/room-cards` instead of letting anyone open a room for free. Once a card is activated, room creation is unlimited until it expires (unchanged from before - the gate is only at the "open a room" step, matching what you described). Extended Room Cards from just 1h/24h to the full set you asked for - 1h, 3h, 6h, 24h, 1 week, 1 month (`types/economy.ts`'s `ROOM_CARD_DURATION_HOURS`/`ROOM_CARD_LABELS`) - and added a "Buy with Coins" section to `/room-cards` (`RoomCardManager.tsx`) so all six are actually obtainable, not just the two that came from daily login/VIP. Prices (`data/cosmetics.ts`'s `ROOM_CARD_PRICES`) are placeholder numbers, flagged as intended to be admin-editable once the admin panel (task in progress) exists. Room owners can now **ban** a player in addition to kick (`lib/rooms.ts`'s new `banPlayer` + `RoomDoc.bannedUids`) - banned players are blocked from rejoining with the same code, unlike a kick. Removed players (kicked or banned) get a real notification: `RoomLobbyClient` detects when its own uid disappears from a still-open room's player list and shows a "You were removed"/"You were banned" screen instead of just silently failing. No new Firestore rules needed - `bannedUids` is an additive field on the existing `rooms/{code}` document, still covered by the current owner-or-member update rule.

- Mindi room modes: Team 2v2 + FFA 1v1 — **Partially done** (2026-07-27), scoped down from the full request on purpose - see below. Mindi private rooms now let the owner pick a mode when creating: **Team 2v2** (the existing default - 4 players, fixed partnerships) or **1v1 FFA** (2 players, no partnership). For Team 2v2, once the room is full the owner sees a live Team A/Team B preview (`lib/rooms.ts`'s new `seatOrder`, owner-adjustable via `setSeatOrder`) and can swap the pairing before starting - "show teams in room before starting" from your request. For 1v1 FFA, added a genuinely new deal function (`mindiEngine.ts`'s `dealMindiHandFFA1v1` - 26 cards each instead of 13, only seats 0/1 in play) and threaded a `numPlayers` field through `MindiOnlineClient` and `checkHandOutcome` (which now takes an optional `totalTricks` param, defaulting to 13 so every existing call site is unaffected) so trick-completion, turn order, and win-condition math are correct for a 2-player hand instead of silently assuming 4. **Deliberately not done yet: 1v1v1 and 1v1v1v1 free-for-all** (3 or 4 individual players, no partnerships). Unlike 1v1 (which happens to reduce to the existing team model, since each "team" has exactly one player), 3/4-player FFA needs genuinely individual per-seat scoring - the current engine only ever tracks tens/tricks by 2-team totals, so a 3rd or 4th individual player would incorrectly get merged into "Team A" or "Team B" with someone they're not actually partnered with. Building that properly needs a non-team scoring model in `mindiEngine.ts` plus dedicated 3-seat/4-seat-individual rendering in `MindiOnlineClient` - a bigger, separate piece of work than reusing what's there, so it's split out rather than rushed. Not yet tested live with real accounts.

- Cosmetic earn sources + VIP pricing — **Done** (2026-07-27). Added the two missing cosmetic categories from your list, **stickers** and **banners** (`types/economy.ts`, `data/cosmetics.ts`), with a few starter items each and full Shop/Inventory wiring (banners get a persistent equip slot like profile frames; stickers are owned-and-used-contextually like emotes, no equip slot, matching how emotes already worked). Wired real, working earn sources instead of only the shop: daily login now grants a cosmetic on top of coins on some days (day 3 → a sticker, day 5 → a banner - `EconomyContext.tsx`'s new `grantCosmeticToCollection` helper, shared so this logic lives in one place), weekly missions can carry a `rewardCosmeticId` (Win 10 Matches → sticker, Reach Gold → banner, Reach Platinum → a Legendary frame, Weekend Champion → an exclusive banner), and the weekly Ranked/Weekend League rank reward (`checkAndClaimWeeklyRank`) now also grants a cosmetic for Gold and Platinum tiers alongside its coin payout - previously that function used a hardcoded coin table instead of the real `RANK_CONFIGS`, now fixed to read from the single source of truth. Exclusive/higher-rarity items remain purchasable with coins in the Shop as before. **VIP pricing**: added a real monthly plan (MVR 350, 30 days) next to the existing weekly plan (MVR 100, 7 days) in the Shop's VIP tab - priced as a "slight discount" vs. four back-to-back weekly passes (4 × 100 = 400) per your spec, with a "SAVE" badge. Selecting either plan calls the same `activateVip(days)` with the right day count; payment is still the existing placeholder (real payment gateway not implemented). Not yet tested live with real accounts.

- Friend messaging (DMs) — **Done** (2026-07-27), needs a Firestore rules deploy - see below. New `lib/messages.ts`: a conversation is a plain `dmConversations/{id}` document (id = the two uids sorted and joined, so both sides land on the same doc with no lookup) with a `messages` subcollection for the actual chat log - same no-backend, client-trusted model as the rest of the app. New `/messages` route (`components/messages/MessagesClient.tsx`): a conversation list (`watchConversations`, live, sorted client-side by last-activity) and a chat view (`?with=uid&name=Name`) with live send/receive (`sendMessage`/`watchMessages`). Added a "Message" button next to each friend on the Friends page, and a "Messages" tab in the bottom nav. **Needs a `firestore.rules` deploy** (`firebase deploy --only firestore:rules`) before this works in production - added rules restricting both the conversation doc and its messages subcollection to just the two participants (checked via a `get()` on the parent doc for the subcollection, since Firestore rules are evaluated per-collection). Caught and fixed one bug before shipping: `watchConversations` initially queried the whole `dmConversations` collection with no server-side filter and relied on client-side filtering - Firestore actually rejects an unfiltered "list" query against a per-document security rule outright, so it needed a real `where("participants", "array-contains", uid)` clause, not just a client-side `.filter()`. Not yet tested live with real accounts.

### Version 1.5
- Collections — **Done** UI (`app/collection`).
- Inventory — **Done** (2026-07-27). New `/inventory` page (linked from Home): a Cosmetics tab showing only *owned* card backs/table themes/frames/emotes/victory animations with equip toggles, and a Room Cards tab reusing the existing (previously unlinked-from-anywhere) `RoomCardManager`. Backed entirely by the existing `playerEconomy/{uid}` document - no new Firestore rules needed. Scope note: stickers and banners aren't cosmetic categories in the data model yet - adding them is bundled into the upcoming "cosmetic earn sources" work so the category plumbing (data model, purchase/equip reducer, shop tab) only gets touched once.
- Achievements — **Done** UI (`app/achievements`), tied to Economy context.
- News & Updates — **Fixed** (2026-07-27). Cards already looked clickable (hover/cursor styling) but had no click handler at all. Tapping a card now opens a detail popup with the full announcement text and date.
- Hall of Fame — **Done** (2026-07-27). New `/hall-of-fame` page (linked from Home) shows an all-time "best ever" list. Ranked by a new `peakTrophies` field (`lib/trophyUpdates.ts`) that only ever increases, rather than current trophies (which drop after a loss) - so a player's Hall of Fame placement can't be lost by a bad week, only improved. Reuses the same no-scheduled-job pattern as Weekend League: no Cloud Function needed, just a live Firestore query ordered by `peakTrophies`. Note: players who haven't played a match since this field was added will show with 0 peak trophies until their next match recalculates it - a one-time backfill gap that resolves itself as people keep playing.
- Seasonal cosmetics — **Done** (2026-07-27). The Shop's "Featured" tab had a countdown timer promising a weekly rotation, but the item list was actually static forever (`isFeatured` was never set on any cosmetic, so the filter silently fell back to "every non-VIP Legendary item", always the same 6). New `lib/cosmeticRotation.ts` picks a deterministic, seeded-random featured set from the current calendar week - every player sees the same set, it changes automatically each week, no backend job needed (same pattern as Weekend League/Hall of Fame). VIP members now get a real 7th featured slot instead of just a decorative "+1 available" message. Also fixed `useSeasonInfo`/`SeasonCard` which hardcoded "Season 1" and a 65% progress bar forever - season number and progress are now computed from the real calendar (one season per month, since app launch).

### Version 2.0
- Clubs — **Done** (2026-07-27), needs a Firestore rules deploy. New `lib/clubs.ts`: a club is a `clubs/{id}` doc (name, tag, description, owner, member list + names + trophy snapshot) capped at 30 members - a player can belong to at most one club at a time, kept deliberately simple rather than juggling multi-club membership. New `/clubs` route (`components/clubs/ClubsClient.tsx`, linked from Home): browse/join/create if you're not in a club yet; once you're a member, a club home with a sortable member list (owner can kick), a live club chat (own `messages` subcollection, members-only), and Leave. If the owner leaves, ownership transfers to another member automatically instead of orphaning the club. Firestore rules added for `clubs` (browsable like the leaderboard; only the owner or a joining member can update the roster) and its `messages` subcollection (members-only, checked via `get()` on the parent club doc) - **needs `firebase deploy --only firestore:rules`** before this works live, same as the DM feature just before it. Not yet tested live with real accounts.
- Spectator mode — **Done** (2026-07-27), needs a Firestore rules deploy. Widened `matches/{matchId}`'s read rule to any signed-in user (writes are still players-only) so a live match can be watched - the same accepted trade-off already documented in `lib/matchmaking.ts` (a savvy user could always inspect raw match data via devtools; the point is the UI never renders it). New `lib/matchmaking.ts`'s `getActiveMatchId(uid)` finds a player's current live match, and new `components/game/SpectateClient.tsx` (route `/spectate?m=matchId`) renders a **read-only, hands-hidden** view: card counts and whose turn it is for both games, plus trump/tens/trick-in-progress for Mindi and stock count/top discard for Gin Rummy - deliberately never the contents of anyone's hand, matching how a player's own client already hides their opponent's hand. Entry point: visiting a player's public profile (`/player?uid=`) now shows a "Currently in a match — Watch Live" banner if `getActiveMatchId` finds one. **Needs `firebase deploy --only firestore:rules`** for the widened `matches` read rule to take effect. Not yet tested live with real accounts.
- New card games — **Pending** (only Mindi + Gin Rummy exist).

- Admin panel — **Done** (2026-07-27), scoped down in one specific way explained below, needs a Firestore rules deploy. Real access control lives in `firestore.rules`'s new `isAdmin()` helper (checks `request.auth.token.email` against a hardcoded allowlist, currently just `sayyu9898@gmail.com` per your instruction) - `lib/admin.ts`'s `isAdminEmail()` is only a client-side convenience for showing/hiding the `/admin` page (linked from Settings → Account, admin-only), not the real security boundary. Six working tabs:
  - **Top-ups**: the coin top-up approval gate you asked for. Buying a coin pack in the Shop now creates a `coinTopupRequests` doc instead of instantly crediting coins; the admin panel lists pending requests with Approve/Reject. A new `CoinTopupWatcher` (mounted once in `MainLayout`) watches the buyer's own requests and self-credits the coins the moment status flips to "approved" - the admin never writes into another player's economy doc directly, keeping `playerEconomy`'s owner-only rule intact. Firestore rules specifically prevent a player from self-approving their own request (they can only ever flip `approved → credited`, never set `approved` themselves).
  - **Season**: force a specific season number, or reset to the automatic calendar-computed value (`hooks/useSeasonInfo.ts` now checks for an admin override first).
  - **Hall of Fame**: add/remove manual entries (a team, a legacy player, etc) shown merged into the real computed ranking; "Reset" clears these manual entries.
  - **Shop**: per-item price override and show/hide toggle, enforced both in the Shop UI and in the actual purchase logic (`EconomyContext.tsx`'s `PURCHASE_COSMETIC`), not just cosmetically displayed.
  - **Missions**: edit the coin reward for any daily/weekly mission template - applies even to already-generated missions, since each generated mission now also stores a stable `templateId` the override is keyed against.
  - **Ranked**: edit the weekly coin reward per rank tier (Bronze/Silver/Gold/Platinum), read by the same `checkAndClaimWeeklyRank` that pays it out.

  **Deliberately not implemented: a true bulk "reset the leaderboard" or "reset every player's trophies to zero."** That would mean rewriting thousands of individual `players/{uid}` documents from the browser with no batching/transaction spanning them and no guarantee it finishes if the admin closes the tab mid-reset - genuinely needs a server-side job (Cloud Function), which this client-only app doesn't have. What's implemented instead (config overrides + additive manual entries) is the part that's actually safe and correct to do from a client. This limitation is documented at the top of `lib/admin.ts` too.

  **Needs `firebase deploy --only firestore:rules`** for every rule in this change to take effect (the `isAdmin()` helper, `appConfig`, `coinTopupRequests`, `hallOfFameManual`). Not yet tested live with a real admin account.

- Language switch (i18n) — **Partially done** (2026-07-27), scoped down on purpose - see below. Real infrastructure is in place, not a placeholder: `AppSettings.language` (`types/index.ts`, persisted the same way as every other setting, in `localStorage` via `SettingsContext`), a translation dictionary + lookup (`lib/i18n.ts`), and a `useTranslation()` hook (`hooks/useTranslation.ts`) that any component can call as `t("key")`, falling back to English and then to the raw key for anything not yet translated so an un-migrated screen never crashes or shows blank text. Dhivehi is correctly wired as right-to-left: `SettingsContext` now sets `document.documentElement.dir`/`lang` whenever the language changes (`lib/i18n.ts`'s `LANGUAGE_DIRECTION` map), the same pattern already used for the dark/light theme class. Added a language picker to Settings (English / ދިވެހި / हिन्दी / বাংলা, four buttons, saves instantly) and applied real translations to the app's highest-traffic surfaces: the bottom nav (all 11 tabs), the Settings page itself, and the Home page header.
  **Deliberately not done: translating every string in the rest of the app.** The app has dozens of components with hardcoded English text (Play, Shop, Missions, Rewards, Leaderboard, Profile, Friends, Messages, Clubs, Admin panel, every game screen, etc.) - translating all of it properly is a large, ongoing content task rather than an architecture problem: each screen just needs its hardcoded strings swapped for `t("new_key")` calls plus four dictionary entries added to `lib/i18n.ts`, following the exact pattern used for the three surfaces above. Tracked as follow-up task #36 rather than rushing a shallow, error-prone translation pass across the whole codebase in one sitting.
  **Translation quality caveat**: the Hindi and Bengali strings are a good-faith best effort for common, simple UI vocabulary. The Dhivehi strings are the least confident of the four - Dhivehi (Thaana script) is a lower-resource language and my training data for it is thin, so several entries are best-effort constructions rather than verified idiomatic phrasing. **None of the three non-English languages have been reviewed by a native speaker** - recommend having one look over `lib/i18n.ts`'s `TRANSLATIONS` object before this ships to real users, especially the Dhivehi entries. Not yet tested live.

- Casual Online matchmaking (auto-teamed, no partner needed) — **Done** (2026-07-27). Mindi Ranked has been duo-only since the 1v1/2v2 split (you must bring a partner) - this adds a second, separate real-multiplayer option for players who don't have one: a new **Casual Online** queue (`components/game/CasualOnlineClient.tsx`, linked from each game's "Casual Mode" section next to vs AI/Pass & Play). It reuses `lib/matchmaking.ts`'s original random-matching `tryFormMatch` (the same function Mindi Ranked used before it required a party) under a new `"casual"` pool - queued players are seated in wait-time order, and `mindiEngine.ts`'s existing `teamOf()` groups seats 0&2 vs 1&3, so a 4-player Mindi casual match auto-assigns a random teammate for free, no new pairing logic needed. Gin Rummy gets the same casual queue too (a plain unranked 1v1), since it's the same primitive at near-zero extra cost. **No trophies, no rank changes, no daily/weekly match-limit consumption** - `MindiOnlineClient`/`GinRummyOnlineClient` now skip `updateMatchResult` entirely when `match.pool === "casual"` (coins and mission progress still apply via `processMatchEnd`, same as vs-AI/Pass & Play), the on-screen header shows "Casual" instead of "Ranked", and the post-match reward popup shows "Casual match — no trophies at stake" instead of a fabricated `+0 Trophies` line (`MatchRewardPopup.tsx` now hides the trophy row entirely when `trophyChange === 0`). Reuses the existing `/ranked/live`-style live view (`RankedLiveClient`, which has no Ranked-specific logic - it just renders the match by id) under a new `/play/[game]/casual/online/live` route. No new Firestore rules needed - `casual` is just a new value for the existing `pool` field on the already-covered `matchmakingQueue`/`matches` collections. Not yet tested live with real accounts.

- Expand i18n translation coverage (round 2) — **Partially done, in progress** (2026-07-27). Extends the language switch infrastructure from before (`lib/i18n.ts`, `useTranslation()`) to a second batch of high-traffic screens: the shared `PageHeader` component's title is used across 14 files/16 call sites (Collection, Achievements, Hall of Fame, Inventory, Weekend League, Shop, Room Cards, Daily Rewards, Missions, Messages, Player Profile, Clubs ×3, Admin/Admin Panel) - translating that one prop everywhere it's passed a static string covered all of them cheaply and consistently (dynamic titles, like a club's actual name, were correctly left alone). Also translated the Play page header/subtitle, the Leaderboard header/subtitle/weekly-rankings banner, and the Friends page's three tab labels (fixed a variable name collision along the way - the tab-mapping callback was itself called `t`, shadowing the new `t()` translation function, so it's renamed to `tabKey`). **Still not translated**: the actual body content of most of these screens (item lists, descriptions, form labels, empty states, etc.) - this pass deliberately covers navigation/entry points first since those are seen on every visit, same reasoning as the original round. Continuing to expand coverage screen-by-screen is still tracked as ongoing work, not fully closed out.

- Expand i18n translation coverage (round 3) — **Partially done, in progress** (2026-07-27). Went one layer deeper into the Shop, Missions, and Daily Rewards screens (previously only their `PageHeader` title was translated): Shop's own header/subtitle, all four tab labels (Featured/Permanent/Coin Packs/VIP Pass), all eight category filter chips, the Equip/Equipped/Purchase buttons, and the "Weekly Featured" section heading; Missions' Daily/Weekly section headers, reset-schedule captions, "completed" counter label, and the all-daily-complete bonus banner; Daily Login Rewards' title/subtitle, streak counter, per-day button labels, claimed-this-cycle progress caption, and the claim popup's "Day N Claimed!"/bonus-card text. Added `lib/i18n.ts` string entries with a simple `{n}`-token convention (`t('key').replace('{n}', String(value))`) for the handful of strings that need to interpolate a number, since `translate()` itself stays a plain lookup rather than growing a templating engine for a handful of call sites. Fixed another `t` variable-name collision (Shop's pending-topup `.find((t) => ...)` callback, renamed to `topup`). **Still not translated**: individual cosmetic item names/descriptions, mission titles/descriptions (both come from data templates, not UI chrome - translating those is a content task, not a code task), the VIP plan panel's benefit list and plan labels, and the Friends/Clubs/Messages/Admin panel screens' body content (search UI, list items, forms). Continuing to expand coverage screen-by-screen remains open, ongoing work.

## Suggested next steps
1. Run `firebase deploy --only firestore:rules` to restore login.
2. Verify the app builds cleanly (`npm run build`) with the uncommitted Economy/Firebase changes, then commit and push so this progress is safe.
3. Decide priority for the next chunk of work — likely candidates: real Firestore-backed leaderboard, real player-vs-player matchmaking, or Friends/Rooms backend.
