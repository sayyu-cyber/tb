{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "//": "Node 18 was DECOMMISSIONED on 2025-10-30 - deploys targeting it are rejected outright, which is why nothing here has ever shipped. Node 20 decommissions 2026-10-30, so 22 is the only choice that avoids doing this again within weeks.",
    "node": "22"
  },
  "//main": "Nested because tsconfig's rootDir is the repo root, so the shared catalogue in ../data can be compiled instead of duplicated. See functions/tsconfig.json.",
  "main": "lib/functions/src/index.js",
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^6.5.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "private": true
}
